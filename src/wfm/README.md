# WebFileManager (vendored)

Explorer-style SD / flash web UI, bundled into ESP32CSI_Vision so sketches do not need a separate Arduino library.

Public include: `#include <WebFileManager.h>` or via `ESP32CSI_Vision.h`.

Upstream / standalone copy may still live in the repo's `WebFileManager/` folder for development; Arduino builds use these sources only.
